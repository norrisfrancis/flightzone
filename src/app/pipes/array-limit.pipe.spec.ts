import { ArrayLimitPipe } from './array-limit.pipe';

describe('ArrayLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
